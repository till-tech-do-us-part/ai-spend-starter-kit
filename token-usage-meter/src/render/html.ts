import type { Flag, Rollups } from "../types.js";
import { attribute, text } from "./escape.js";
function bars(entries: [string,{totalTokens:number}][]): string { const max=Math.max(0,...entries.map(([,v])=>v.totalTokens)); if(!entries.length)return '<p class="empty">No usage data.</p>'; return `<svg viewBox="0 0 600 ${entries.length*34}" role="img" aria-label="Token usage bars">${entries.map(([k,v],i)=>{const w=max?Math.round(v.totalTokens/max*430):0;return `<text x="0" y="${i*34+20}">${text(k)}</text><rect x="150" y="${i*34+5}" width="${attribute(w)}" height="20"/><text x="${attribute(160+w)}" y="${i*34+20}">${text(v.totalTokens)}</text>`}).join("")}</svg>`; }
export function renderHtml(r:Rollups,flags:Flag[]):string { const ratio=r.summary.cacheHitRatio===null?"n/a":`${(r.summary.cacheHitRatio*100).toFixed(1)}%`;return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Token usage meter</title><style>body{font:15px system-ui;margin:2rem;max-width:1000px;color:#17202a}.tiles{display:flex;gap:1rem;flex-wrap:wrap}.tile{padding:1rem;background:#f3f6f8;border-radius:8px;min-width:130px}.tile b{display:block;font-size:1.4rem}svg{width:100%;max-height:500px}rect{fill:#536dfe}svg text{font-size:12px;fill:#17202a}.flag{border-left:4px solid #d97706;padding:.5rem 1rem;margin:.5rem 0}.empty{color:#64748b}</style></head><body><h1>Token usage meter</h1><div class="tiles"><div class="tile">Total tokens<b>${text(r.summary.totalTokens)}</b></div><div class="tile">Cache hit<b>${text(ratio)}</b></div><div class="tile">Sessions<b>${text(r.summary.sessionCount)}</b></div><div class="tile">Top project<b>${text(r.summary.topProject??"n/a")}</b></div><div class="tile">Top model<b>${text(r.summary.topModel??"n/a")}</b></div></div><h2>Tokens by day</h2>${bars(Object.entries(r.byDay))}<h2>By project</h2>${bars(Object.entries(r.byProject))}<h2>By model</h2>${bars(Object.entries(r.byModel))}<h2>Flags</h2>${flags.length?flags.map(f=>`<div class="flag"><b>${text(f.id)} — ${text(f.lever)}</b><p>${text(f.message)} ${text(f.evidence)}</p><p>Guide habit: ${text(f.guideLink)}</p></div>`).join(""):'<p class="empty">No waste flags.</p>'}</body></html>`;}
